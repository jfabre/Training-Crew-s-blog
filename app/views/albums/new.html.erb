<h1>New album</h1>

<% form_for(@album) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <select name="images" type="multiline">
    <% @images.each do |x| %>
      <option value="<%= x.id %>"><%= x.name %></option>
    <% end %>
  </select>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', albums_path %>