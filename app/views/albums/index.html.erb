<h1>Listing albums</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @albums.each do |album| %>
  <tr>
    <td><%=h album.name %></td>
    <td><%= link_to 'Show', :action => :show, :id => album.id %></td>
    <td><%= link_to 'Edit', :action => :edit, :id => album.id %></td>
    <td><%= link_to 'Destroy', :action => :destroy, :id => album.id %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New album', :action => :new %>